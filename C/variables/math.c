#include <stdio.h>
#include <string.h>
#include <math.h>


int main(){

	double O = 9;
	double A = sqrt(O);
	double B = pow(O, A);  
	int C = round(3.14);
	int D = ceil(3.14);
	int E = floor(3.99);
	double F = 1;
	double G = 1;
	double H = 1;
	double I = 1;
	double J = 1;

	printf("\n%d", E);

	
	return 0;
}
