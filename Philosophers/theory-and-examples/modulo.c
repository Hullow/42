#include <stdio.h>
#include <stdlib.h>

int main()
{
	int a = 0;
	int b = 1;
	int c = 2;

	if (a % 2)
		printf("0 mod 2 is true\n");
	if (b % 2)
		printf("1 mod 2 is true\n");
	if (c % 2)
		printf("2 mod 2 is true\n");
	return (0);
}